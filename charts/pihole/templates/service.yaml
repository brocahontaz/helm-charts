apiVersion: v1
kind: Service
metadata:
  name: {{ include "pihole.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pihole.labels" . | nindent 4 }}
  annotations:
  {{- if .Values.commonAnnotations }}
    {{- include "yggio.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
  {{- if .Values.service.web.annotations }}
    {{- include "yggio.tplvalues.render" ( dict "value" .Values.service.web.annoations "context" $) | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.web.type }}
  {{- if (or (eq .Values.service.web.type "LoadBalancer") (eq .Values.service.web.type "NodePort")) }}
  externalTrafficPolicy: {{ .Values.service.web.externalTrafficPolicy | quote }}
  {{- end }}
  sessionAffinity: {{ .Values.service.web.sessionAffinity }}
  ports:
    {{- with .Values.service.web.ports }}
      {{- toYaml  . | nindent 4 }}
    {{- end }}
  selector:
    {{- include "pihole.selectorLabels" . | nindent 4 }}
