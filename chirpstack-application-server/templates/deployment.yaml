apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "chirpstack-application-server.fullname" . }}
  labels:
    {{- include "chirpstack-application-server.labels" . | nindent 4 }}
spec:
{{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
{{- end }}
  selector:
    matchLabels:
      {{- include "chirpstack-application-server.selectorLabels" . | nindent 6 }}
  template:
    metadata:
    {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      labels:
        {{- include "chirpstack-application-server.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "chirpstack-application-server.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: ui-port
              containerPort: {{ .Values.containerPorts.uiPort }}
              protocol: TCP
            - name: join-port
              containerPort: {{ .Values.containerPorts.joinPort }}
              protocol: TCP
            - name: http-port
              containerPort: {{ .Values.containerPorts.httpPort }}
              protocol: TCP
            - name: monitoring-port
              containerPort: {{ .Values.containerPorts.monitoringPort }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http-port
          readinessProbe:
            httpGet:
              path: /
              port: http-port
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
